[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: tests that hit external services (skipped by default)
