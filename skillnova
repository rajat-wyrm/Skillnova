#!/usr/bin/env bash
# Wrapper so the SkillNova CLI is invokable as `skillnova` (or `./skillnova`)
# after `pip install -e .`. It just delegates to scripts/cli.py.

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/scripts/cli.py" "$@"